mutable struct Body
    # TODO
end

"""
    update!(b::Body, dt, ts, correction)

    - `dt::Float`: time delta
    - `ts::Float`: time scale
    - `correction::Float`: ?
"""
function update!(b::Body, dt, ts, correction)
    # TODO
end

function apply_force!(b::Body, force, position)
    # TODO
end
